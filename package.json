{
  "name": "webgpu-doom-pathtracer",
  "version": "0.1.0",
  "description": "WebGPU path-traced Doom renderer",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:component": "vite build --config vite.config.component.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
